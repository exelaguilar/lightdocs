<?php

declare(strict_types=1);

// Trusted installations may register a small custom Markdown directive here:
// 'name' => static fn (array $attributes, string $html, string $source): string => $html,
// Custom renderers receive already-safe Markdown HTML but remain trusted PHP code.
return [];
