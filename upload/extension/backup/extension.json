{
  "schema_version": 1,
  "name": "backup",
  "class": "Extension\\Backup\\Extension",
  "version": "1.0.0",
  "description": "Optional local ZIP backup provider for editable source, assets, and revisions.",
  "type": "developer",
  "default_enabled": false,
  "contexts": ["admin"],
  "navigation": [{"section": "Tools", "label": "Backups", "route": "/admin/backups", "active": "backups", "icon": "export", "sort_order": 20}],
  "settings": [
    {"key": "retention_days", "label": "Retention period", "type": "number", "default": 30, "min": 1, "max": 3650, "description": "Delete backup archives older than this many days."},
    {"key": "max_archives", "label": "Maximum archives", "type": "number", "default": 20, "min": 1, "max": 500, "description": "Keep at most this many recent backup archives in addition to applying the retention period."},
    {"key": "include_revisions", "label": "Include local revisions", "type": "boolean", "default": true, "description": "Include the private revision history directory in each backup."},
    {"key": "include_uploads", "label": "Include uploaded assets", "type": "boolean", "default": true, "description": "Include uploaded images and files referenced by documentation."},
    {"key": "include_database", "label": "Include application database", "type": "boolean", "default": true, "description": "Include accounts, roles, extension settings, events, and audit records."},
    {"key": "include_environment", "label": "Include environment file", "type": "boolean", "default": false, "description": "Include the private deployment environment file in the archive."}
  ]
}
